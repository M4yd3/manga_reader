enum BaseScope {
  library,
  reading,
  toRead,
  haveRead,
  favorite,
  haventRead,
  search,
  authors,
  series
}

enum ToggleParam { toRead, haveRead, favorite }

enum SortingOrder { title, author, size }
